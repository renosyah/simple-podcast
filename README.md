# Simple Podcast Project

Simple podcast core api adalah backend untuk aplikasi streaming lagu dan podcast menggunakan golang

* how to run via local fo golang dev

```

go build main.go && ./main --config=.server.toml

```